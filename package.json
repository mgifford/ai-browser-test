{
  "name": "ai-browser-test",
  "version": "1.0.0",
  "description": "AI Browser Capability Demo — utility tests",
  "private": true,
  "scripts": {
    "test": "node --test tests/*.test.js",
    "test:e2e": "playwright test"
  },
  "engines": {
    "node": ">=18"
  },
  "devDependencies": {
    "@axe-core/playwright": "^4.11.2",
    "@playwright/test": "^1.59.1",
    "serve": "^14.2.6"
  }
}
